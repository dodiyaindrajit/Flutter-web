name: flutter_web_expenses
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  google_fonts: ^3.0.1
  responsive_sizer: ^3.1.0
  provider: ^6.0.3
  fl_chart: ^0.55.0
  sticky_and_expandable_list: ^1.1.2
  grouped_list: ^5.1.2
  flutter_calendar_carousel: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

  assets:
    - asset/images/